.gradle/
build/
app/build/
local.properties
.idea/
*.iml
.DS_Store

# Готовые сборки в репозитории не храним: APK собирает GitHub Actions
# и выдаёт как артефакт workflow + релиз (см. .github/workflows/).
*.apk
*.aab

# Логи сборки
*.log
